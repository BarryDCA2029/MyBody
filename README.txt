MY BODY V1 prototype
Open index.html in a browser. Data is stored locally with localStorage.
Seed weight data uses only user-confirmed measurements. No invented intermediate weights.
Note: photo capture/AI calorie estimation and native scheduled notifications require the next implementation stage/backend or browser capabilities; this prototype does not pretend those are active.
